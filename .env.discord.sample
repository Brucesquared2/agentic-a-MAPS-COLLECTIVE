# Optional Discord bridge environment sample
# Copy values into your .env if you plan to run the bridge locally

DISCORD_BOT_TOKEN=your_discord_bot_token_here
DISCORD_CHANNEL_ID=the_channel_id_to_post_to
# Optional Pushgateway or HTTP sink that accepts Prometheus-style metrics
BRIDGE_METRICS_URL=http://pushgateway:9091/metrics/job/discord_bridge
# Enable verbose echo of events in the bridge container
# BRIDGE_DEV_ECHO=true
